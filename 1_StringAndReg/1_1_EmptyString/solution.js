function DoSomething(string) {
  // Что-то делаем с string
}

function DoSomething(string) {
  if (
    typeof string === "string" && 
    string.trim().length > 0 
) {
    // Что-то делаем с string
  }
}


const string = new String("string");

